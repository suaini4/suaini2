/**
 * This file is kept as a placeholder to maintain compatibility with any imports.
 * The application now uses Supabase for data persistence instead of Firebase.
 */

// Mock Firebase objects to prevent errors in existing code
export const auth = {};
export const db = {};

export default {};
